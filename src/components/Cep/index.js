import React from "react";
import styles from "./styles.css"
import Form from "../Form";

export const Cep=()=>{
    return <section className="CepContainer">
            <Form></Form>
            </section>
}

export default Cep;